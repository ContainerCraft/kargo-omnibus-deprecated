all:
  hosts:
    deploy:
  children:
    kargo:
      hosts:
        pod:
          ansible_host: 192.168.1.105
      vars:
        ansible_port: 22
        ansible_user: root
        ansible_connection: ssh
    localhost:
      hosts:
        konductor:
          ansible_user: root
          ansible_connection: local
